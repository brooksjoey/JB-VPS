# JB-VPS

### Directory Diagram
```markdown
JB-VPS/
├── scripts/
│   ├── env-manager.sh      # NEW: Interactive environment manager
│   ├── setup_vps.sh        # VPS initialization
│   └── webhost.sh          # Web hosting management
├── secure/
│   └── environments/       # Encrypted environment storage
│       ├── personal.env.gpg
│       ├── ai-projects.env.gpg
│       └── red-team.env.gpg
└── tools/
    └── env-loader.sh       # Environment loading utilities
```